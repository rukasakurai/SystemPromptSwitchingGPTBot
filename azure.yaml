name: SystemPromptSwitchingGPTBot
resourceGroup: rg-systempromptbot-staging

infra:
  provider: bicep
  path: infra
  module: main

services:
  backend:
    project: ./app
    language: dotnet
    host: appservice