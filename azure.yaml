name: SystemPromptSwitchingGPTBot

infra:
  provider: bicep
  path: infra
  module: main

hooks:
  preprovision:
    windows:
      shell: pwsh
      run: ./infra/hooks/preprovision.ps1
    linux:
      shell: pwsh
      run: ./infra/hooks/preprovision.ps1

services:
  backend:
    project: ./app
    language: dotnet
    host: appservice